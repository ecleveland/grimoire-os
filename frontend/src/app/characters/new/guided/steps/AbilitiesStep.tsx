import StepPlaceholder from '../_components/StepPlaceholder';

/** Step 3 — Ability scores (array / point-buy / roll) + background increases. Stub for VEG-381. */
export default function AbilitiesStep() {
  return (
    <StepPlaceholder
      title="Abilities"
      summary="Assign ability scores via standard array, point-buy, or rolling, plus background increases."
      ticket="VEG-381"
    />
  );
}
